# spec.md — 프로젝트 고유값

이 파일을 `spec.md` 로 복사해 채운다. 규칙은 CLAUDE.md 에, 고유값은 여기에만 둔다.
`<>` 표시가 남아 있으면 미기재로 간주하고 Claude 는 착수하지 않는다.
쓰지 않는 항목은 지우지 말고 "해당 없음" 이라고 적는다 — 빈 칸과 구분하기 위해서다.

## 1. dispatch_command

워커 실행 명령. **10분 룰 안쪽의 타임아웃 플래그를 반드시 포함한다**(CLAUDE.md §3).
타임아웃이 없으면 Claude 는 디스패치하지 않는다.

Codex
```
<예: codex exec --full-auto --json --model <모델> -c model_reasoning_effort=<effort> "<브리프>">
```

agy
```
<예: node .claude/skills/agy-delegate/scripts/relay.mjs --brief <파일> --cd <경로> --print-timeout 7m --timeout 8m>
```

플래그 표기는 프로젝트에서 처음 쓸 때 1회 `<워커> --help` 로 실제 표기를 확인한다.
모델명 오타는 조용히 기본 모델로 폴백돼 게이트 판정을 오염시킨다.

## 2. 모델·노력도 라우팅

| 용도 | 워커 | 모델 | effort |
|---|---|---|---|
| 설계·아키텍처 | Codex | <> | <> |
| 어려운 코딩 | Codex | <> | <> |
| 고위험 정적 리뷰 | Codex | <> | <> |
| 단순 코딩 | agy | <> | <> |
| 단순 QA·리서치·잡무 | agy | <> | <> |
| 저위험 정적 리뷰 | agy | <> | <> |

## 3. 게이트 명령 3종

Claude 가 직접 실행한다. 워커의 self-report 를 대신 쓰지 않는다.
아직 코드가 없는 신규 리포면 "해당 없음" 이라고 적는다.

```
verify_typecheck: <예: npm run typecheck>
verify_lint:      <예: npm run lint>
verify_test:      <예: npm test>
```

E2E 실행 명령 (게이트②). 없으면 "없음" — 이 경우 동적 QA 는 자동 통과가 금지되고
`queue/qa/` 에서 사람 확인을 기다린다(CLAUDE.md §4).
```
verify_e2e: <예: npx playwright test>
```

## 4. 코딩 규약

이 프로젝트만의 금지·강제 사항. 워커 브리프에 원문 그대로 옮겨 적는 대상이다.
일반론(“읽기 쉽게 짜라”)은 쓰지 않는다. 어겼는지 diff 로 판정 가능한 것만 쓴다.

- 디렉터리 구조: <>
- 명명 규칙: <>
- 금지 API·라이브러리: <>
- 강제 API·라이브러리: <>
- 환경변수 규칙: <>
- 기타: <>

## 5. 보고 채널

<없으면 "대화로 보고">

## 6. 배포 명령·대상

사람 승인 없이 배포하지 않는다(CLAUDE.md §7). 비어 있으면 "배포하지 않는다" 로 확정된다.

- 대상 환경: <>
- 배포 명령: <>
- 배포 후 회귀 담당: Codex (CLAUDE.md §4)

## 7. 운영 데이터 접근 범위

- QA 테스트 계정: <>
- 건드리면 안 되는 데이터: <>
- 접근 금지 환경: <>

## 8. 병렬 격리 (선택)

동시 디스패치할 때 워커별 작업 경로를 어떻게 나눌지. 지정이 없으면 Claude 는
동시 디스패치를 하지 않고 순차 처리한다.

- 격리 방식: <예: git worktree>
- 경로 규칙: <예: worktrees/<작업 id>>
- 동시 실행 상한: <>

## 9. 세션 처리 상한 (선택)

한 세션에서 처리할 큐 파일 수. 비어 있으면 기본값 5건이 적용된다.
건당 작업이 크면 낮추고, 문서·잡무 위주 큐면 올린다.

- 세션 처리 상한: <>

## 10. 커밋 규약 (선택)

- 메시지 접두사: <예: [기능] [수정] [규칙]>
- 브랜치 규칙: <>
