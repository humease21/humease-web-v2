# POST-LAUNCH MANUAL CHECKLIST

- 작성: 2026-09-07 KST
- 대상: `https://www.humease.com` (운영 중)
- 상태: **개발·배포 완료.** 아래는 개발 미완료 항목이 아니다.

## 이 문서의 성격

여기 있는 항목은 **코드로 끝낼 수 없는 것들**이다. 계정 권한, 사람 눈, 대표님의 사실 판단이 있어야 한다. 개발 큐를 열어둔 채 기다릴 이유가 없어 별도 문서로 분리했다.

개발 요청 `HUMEASE-WEB-20260906-SEARCH-02` 는 `queue/done` 으로 종료했다. 새 결함이나 대표님의 명시적 수정 요청이 생기면 그때 신규 작업으로 연다.

**완료 판단 기준** — 이 체크리스트가 남아 있다고 해서 사이트가 미완성인 것이 아니다. 반대로, 이 체크리스트를 다 채웠다고 검색 노출·AI 인용이 보장되는 것도 아니다.

## A. 검색 도구 계정 — 권한 필요

`GitHub 계정을 바꿨다고 검색 도구 권한이 자동 이동하지 않는다.` 저장소만 바뀌고 도메인은 그대로이므로 **Search Console 주소 변경 도구를 실행하지 않는다.** 기존 속성과 인증 태그를 삭제하지 않는다.

| # | 항목 | 확인할 것 |
|---|---|---|
| A-1 | Google Search Console 소유권 | 기존 속성이 살아 있는지. 인증은 DNS TXT 2건이라 도메인 전환과 무관하게 유효하다 |
| A-2 | 사이트맵 제출 | `https://www.humease.com/sitemap.xml` (19 URL) |
| A-3 | Google 생성형 AI 포함 설정·상속 | OBS-01. 계정에서만 확인 가능 |
| A-4 | Google AI 보고서 / 일반 Performance 구분 | OBS-02. 두 지표를 합산하지 않는다 |
| A-5 | Bing Webmaster Tools 소유권 | 구 사이트에도 `BingSiteAuth.xml` 이 없었다. DNS `MS=ms42929545` 로 시도 |
| A-6 | Bing AI 지표·제공 상태 | OBS-03. 제공 범위와 해석 한계를 기록 |
| A-7 | 네이버 서치어드바이저 소유권 | 인증 meta `naver-site-verification` 은 구 사이트 값 그대로 보존돼 있다 |
| A-8 | 네이버 수집·색인 / AI 인용 구분 | OBS-04. 둘을 같은 지표로 보지 않는다 |

**IndexNow 는 완료했다.** 2026-09-06T15:16:41Z, HTTP 202, 19건 제출. 202 는 접수이며 색인이 아니다. 재전송은 내용이 실제로 바뀐 URL만 대상이며 자동으로 걸러진다.

## B. 실기기 육안 QA — 사람 눈 필요

자동 검사로 대체할 수 없다. 실제 기기에서 본다.

| # | 항목 | 기준 |
|---|---|---|
| B-1 | 반응형 (UX-01) | 1440 · 1920 · 768 · 390 · 320px. 긴 제목·긴 FAQ 답변이 깨지지 않는지 |
| B-2 | 접근성 (UX-02) | 키보드만으로 전체 탐색, JS 끈 상태에서 본문 접근, `prefers-reduced-motion` |
| B-3 | 성능 회귀 (PERF-01) | 동일 조건 비교. 랩 지표와 실사용 지표를 구분 |
| B-4 | 모바일 메뉴·네비 드롭다운 | 과거 두 번 결함이 났던 지점 |

## C. 대표님 사실 판단 — 승인 필요

미승인 상태라 **현재 사이트에 표시하지 않고 있다.** 승인 전까지 이 상태가 맞다.

| # | 항목 | 현재 | 필요한 판단 |
|---|---|---|---|
| C-1 | Microsoft MVP 표현 | 미표시 | 사용할지, 어떤 문구로 할지. 구 사이트 `llms.txt` 에 있던 "Microsoft MVP 출신 전문가" 는 승인 전이라 제외했다 |
| C-2 | 회사 정보 | 미표시 | 대표자 · 사업자등록번호 · 주소. 승인값 확인 전 표시하지 않는다 |
| C-3 | A02–A07 이미지 | 기존 자산 사용 중 | 새로 만든 이미지로 교체할지. **이미지 생성·재생성은 범위 밖**이므로 교체 시 파일을 주셔야 한다 |
| C-4 | 포트폴리오 4건 공개 범위 | 4건 모두 published | 맘이음만 `개발 중` 표기. AnyBuild·HairAI·내친구 케이는 단계 표기 없음. 이대로 둘지 |
| C-5 | 문의 기능 | `CONTACT_MODE = 'disabled'` | 실제 어댑터 선택. 정적 호스팅이라 Supabase 직접 insert / 외부 폼 서비스 / 별도 함수 중 택1 |
| C-6 | 개인정보처리방침·약관 | 페이지 없음 | 문의를 살리면 개인정보 수집이 생긴다. C-5 와 함께 판단 |
| C-7 | 구 저장소 `Humease/Homepage` | 그대로 보존 | archive 할지 유지할지. 삭제는 권하지 않는다 |

## D. 운영 중 지켜볼 것

| # | 항목 | 비고 |
|---|---|---|
| D-1 | TLS 인증서 | 2026-10-23 만료. GitHub 이 자동 갱신한다. 갱신 실패 시에만 조치 |
| D-2 | 색인 진행 | 접수·수집·색인·노출·AI 인용을 **각각 구분해서** 본다. 하나로 합산하지 않는다 |
| D-3 | DNS | `www` CNAME → `humease21.github.io.` 완료. apex A 4건·MX·SPF·TXT 는 전환과 무관하게 그대로 |

## E. 롤백 정보 (문제 발생 시)

| 항목 | 값 |
|---|---|
| `www` CNAME 이전 값 | `humease.github.io.` |
| 이전 Custom Domain 저장소 | `Humease/Homepage` |
| 전체 DNS 기준선 | `docs/reports/CUTOVER_STEP2_DNS_BASELINE.md` |

되돌릴 때는 **CNAME 과 구 저장소 Custom Domain 재설정을 함께** 해야 한다. 하나만 하면 계속 404 다.

## 참고 문서

- `docs/reports/G5_PRODUCTION_SEARCH_READINESS.md` — 운영 도메인 실측, 인수 기준 판정
- `docs/reports/G3_SEO_AEO_GEO.md` — 산출물 기준 구현 보고
- `docs/reports/CUTOVER_STEP2_DNS_BASELINE.md` — DNS 기준선·롤백
- `docs/07_RELEASE_AND_ROLLBACK.md` — 릴리스·롤백 절차
