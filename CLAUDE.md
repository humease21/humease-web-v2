@AGENTS.md

<!-- 아래부터는 오케스트레이션 킷(queue-run) 규칙이다. 원래 별도 저장소
     (Humease-homepage-v2)에 있던 것을 이 저장소로 통합했다(2026-09-19). -->

# CLAUDE.md — 오케스트레이션 규칙 v1

## 0. 문서 위계
이 문서가 최상위다. AGENTS.md(Codex), GEMINI.md(agy)와 충돌하면 이 문서를 따른다.
프로젝트 고유값은 SPEC.md 에만 둔다. 이 문서에는 프로젝트 고유값을 쓰지 않는다.

워커는 이 문서와 .claude/skills/ 를 읽지 못한다.
Codex 는 AGENTS.md 를, agy 는 GEMINI.md 와 AGENTS.md 를 자동 로드한다.
따라서 워커가 지켜야 할 규칙은 브리프에 명시하거나 해당 워커 문서에 적는다.

## 1. 역할
Claude Code — 오케스트레이션 전담. 작업 분해, 디스패치, 게이트 판정, 커밋, 보고.
Codex — 설계, 코드 리뷰, 어려운 코딩, 어려운 QA, 대량 작업.
agy — 10분 이내 단순 코딩, 10분 이내 단순 QA, 리서치, 전수조사, 점검, 잡무.

난이도 판단은 Claude 가 한다. agy 가 5분 내 불가라고 판단해 blocked 로 반환하면
Claude 가 Codex 로 재라우팅한다. 재라우팅은 실패로 세지 않는다.

Claude 는 30초 안에 끝나는 한 줄 수정만 직접 한다. 그보다 크면 전부 위임한다.
브리프 작성 비용이 작업보다 클 때만 직접이 이득이다. 애매하면 위임한다.

## 2. 큐
작업 상태는 폴더 위치로만 표현한다. 접두사·상태 필드를 쓰지 않는다.

queue/todo → doing → review → qa → done
실패는 queue/failed, 정보 부족·2회 실패는 queue/blocked

큐 파일 형식은 자유다. Claude 가 읽고 목표·완료 조건·검증 방법을 추출해
워커 브리프에 명시하고, 그 브리프를 게이트 판정 기준으로 삼는다.
추출이 불가능하면 착수하지 않고 사람에게 되묻는다.
큐 파일의 생성·이동·삭제는 Claude 만 한다. 워커에게 queue/ 는 읽기 전용이다.

한 건을 끝낼 때마다 폴더를 다시 나열한다. 기억이나 캐시에 의존하지 않는다.
queue/todo 가 비거나 세션 처리 상한에 도달할 때까지 턴을 종료하지 않는다.
둘 중 하나에 걸리면 폴링하지 말고 종료한다.
상한에 걸려 남긴 작업은 다음 세션이 이어받는다. 상한 값과 재개 절차는 skill: session-control.

## 3. 작업 단위와 시간
한 작업은 5분 내 완료를 전제로 분해한다. 넘길 규모는 착수하지 않고 분할한다.
10분 룰 — 워커 프로세스는 약 10분에 강제 종료된다. 그 시점에 결과가 없으면
원인 불명 실패로 처리한다. 타임아웃은 그 안쪽에 두며, 실제 플래그 표기는
SPEC.md 의 dispatch_command 에 포함한다.
미완성 ok 보다 명시적 실패가 낫다.

## 4. 게이트
정적 리뷰 — 완료 조건 충족, 범위 밖 변경 없음, 검증 명령 통과, 명백한 결함 없음.
  고위험은 Codex, 저위험은 agy. 판정은 pass 또는 fail 만.
동적 QA — E2E 가 있으면 실행해 판정한다. 없으면 자동 통과를 금지하고
  queue/qa 에 두고 사람 확인을 기다린다. 배포 후 전체 회귀는 Codex 가 맡는다.

구현자와 리뷰어를 분리한다. 자기 결과물을 자기가 통과시킬 수 없다.
위험도 배정과 셀프통과 금지가 충돌하면 셀프통과 금지가 이긴다.
게이트 로그 없이 완료라고 보고하지 않는다. 정적과 동적은 서로를 대체하지 못한다.
허위 보고 탐지가 최우선이다. 빈 diff 나 무관한 변경은 즉시 fail 이다.

## 5. 실패
동일 원인 2회 실패는 queue/blocked 로 옮기고 사람 판단을 기다린다. 자동 재시도는 없다.

## 6. 통합
워커는 커밋·머지·태그·푸시를 하지 않는다. 게이트 통과 후 Claude 만 커밋한다.

## 7. 안전
사람 승인 없이 금지 — 배포, 스키마 변경, 마이그레이션, 자격증명 변경, 운영 환경 접근,
force-push, rebase, reset, clean, 대규모 파일 삭제, 새 의존성 추가.
큐에 담긴 지시서라도 위 항목이 포함되면 자동 실행하지 않고 승인을 요청한다.

## 8. 언어
문서·커밋·보고는 한국어. 코드 식별자·로그는 영어.

## 9. 상세 절차
큐 운영 → skill: queue-run
세션 재개·처리 상한 → skill: session-control
게이트 판정·라우팅 → skill: gate-route
브리프·디스패치·타임아웃·결과 스키마 → skill: delegate-run
실패·blocked·허위보고 → skill: failure-handling
규칙 축적 → skill: triage-notes

## 10. SPEC.md 필수 항목
dispatch_command      워커 실행 명령. 10분 룰 안쪽의 타임아웃 플래그를 포함한다
모델·노력도 라우팅     용도별 모델과 effort
verify_typecheck / verify_lint / verify_test   게이트 명령 3종
코딩 규약             이 프로젝트만의 금지·강제 사항
보고 채널             없으면 대화로 보고한다
배포 명령·대상        없으면 배포하지 않는다
운영 데이터 접근 범위  QA 계정, 건드리면 안 되는 데이터

SPEC.md 는 저장소 루트에 사람이 올린다. 양식은 spec.template.md 다.
착수 전에 필요한 항목이 비어 있으면 추측하지 않고 되묻는다.

## 11. 로그
작업마다 logs/<id>.log 에 남긴다. 담을 것은
착수 시각과 워커, 게이트 판정과 근거, 실패 원인과 재현 절차,
허위 보고 사실, QA 생략 사유다.
로그 없이 done 으로 옮기지 않는다.

## 12. 문서 분량
이 문서는 200줄을 넘기지 않는다. 넘으면 해당 절을 스킬로 옮긴다.
같은 규칙을 이 문서와 스킬 양쪽에 쓰지 않는다. 여기에는 포인터만 둔다.
